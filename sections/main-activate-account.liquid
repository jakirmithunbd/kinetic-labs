{{ 'component-content.css' | asset_url | stylesheet_tag }}

<div class="section color-{{ section.settings.color_scheme }}" style="{% render 'section-style', st: 64, sb: 96 %}">
  <div class="page-width">
    <div class="auth-layout">
      <h1 class="h3">{{ 'customer.activate_account.title' | t }}</h1>
      <p class="text-caption text-muted">{{ 'customer.activate_account.subtext' | t }}</p>

      {%- form 'activate_customer_password', novalidate: 'novalidate' -%}
        {%- if form.errors -%}
          <div class="form-status form-status--error" role="alert" tabindex="-1" autofocus>
            {{ form.errors | default_errors }}
          </div>
        {%- endif -%}

        <div class="field">
          <label class="field__label" for="ActivatePassword">
            {{- 'customer.activate_account.password' | t -}}
          </label>
          <input id="ActivatePassword" class="field__input" type="password" name="customer[password]"
            autocomplete="new-password" required>
        </div>

        <div class="field">
          <label class="field__label" for="ActivatePasswordConfirm">
            {{- 'customer.activate_account.password_confirm' | t -}}
          </label>
          <input id="ActivatePasswordConfirm" class="field__input" type="password"
            name="customer[password_confirmation]" autocomplete="new-password" required>
        </div>

        <button type="submit" class="button">{{ 'customer.activate_account.submit' | t }}</button>
        <button type="submit" name="decline" class="button button--secondary">
          {{- 'customer.activate_account.cancel' | t -}}
        </button>
      {%- endform -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "t:customer.activate_account.title",
  "tag": "section",
  "settings": [
    { "type": "color_scheme", "id": "color_scheme", "label": "t:shared.colour_scheme", "default": "scheme-1" }
  ]
}
{% endschema %}
